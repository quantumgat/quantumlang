[package]
name = "qtlc1-tests"
version = "0.1.0"
edition = "2026"

[library]
importRoot = "qtlc1_tests"

[targets.app]
kind = "bin"
main = "main.qn"
backend = "native"

[source]
roots = ["..", "."]
exclude = ["build/**", "tests/build/**"]
