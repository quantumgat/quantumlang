pub QidxWriter {
  emitted: i64
  indexPath: string
}

impl QidxWriter {
  pub QidxWriter(emitted: i64, indexPath: string) =
    This { emitted, indexPath }
}

impl view QidxWriter {
  pub deterministic => true
  pub wroteAny => emitted > 0
  pub valid => emitted >= 0 && indexPath != ""
}
